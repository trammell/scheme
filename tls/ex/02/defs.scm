; (Exercise 2.1) What are the values of (lat? l1), (lat? l2), and
;     (lat? l3)?

(define l1 '(german chocolate cake))
(define l2 '(poppy seed cake))
(define l3 '((linzer) (torte) ()))
(define l4 '((bleu cheese) (and) (red) (wine)))
(define l5 '(() ()))

(define a1 'coffee)
(define a2 'seed)
(define a3 'poppy)

