; (Exercise 2.4) Most Lisp dialects have an "(if-...)" form.  In general an
; "(if-...)" form looks like "(if aexp bexp cexp)".  When aexp is true,
; "(if aexp bexp cexp)" is bexp; when it is false, "(if aexp bexp cexp)" is
; cexp.  For example,
;
;     (cond (null? l)


; Rewrite all the functions in the chapter using "(if ...)" instead of
; "(cond...)".

